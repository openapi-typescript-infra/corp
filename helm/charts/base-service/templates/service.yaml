apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
spec:
  type: ClusterIP
  ports:
    - name: http-port
      port: 80
      targetPort: 8000
    - name: metrics-port
      port: 3000
      targetPort: 3000
  selector:
    app: {{ .Values.name }}
