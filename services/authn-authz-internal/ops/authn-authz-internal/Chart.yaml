apiVersion: v2
name: authn-authz-internal
type: application

# This is the version of the chart. Convention is to put the
# base image version here.
version: 2.6.0

# This is the version number of the application being deployed.
# It will be set automatically by the CI/CD pipeline
appVersion: "1.0.0"

dependencies:
  - name: base-service
    version: 1.0.0
    repository: 'file://../../../../helm/charts/base-service'
