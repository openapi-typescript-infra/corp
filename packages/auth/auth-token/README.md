# @justtellme/auth-token

This package manages creating and reading the x-auth-token header which is the internal header responsible for authentication and the foundation for authorization. It is primarily consumed by @justtellme/web-auth but can be used standalone to create tokens.
