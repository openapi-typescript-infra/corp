# @justtellme/local-runner

A helper to run multiple services in a single terminal tab. It lists all the services in the monorepo's services directory and presents numeric shortcuts to start each of them. It will then show the logs of all of them, but allow filtering display by service. You can also shut down and restart each service as desired.