type: object
required:
  - name
  - version
properties:
  name:
    type: string
    description: The calling client name
  version:
    type: string
    description: The calling client version in semver format
