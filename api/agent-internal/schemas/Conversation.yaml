type: object
required:
  - conversation_id
  - status
properties:
  conversation_id:
    type: string
  status:
    type: string
    enum:
      - active
      - archived
      - deleted
  created_at:
    type: string
    format: date-time
  updated_at:
    type: string
    format: date-time
