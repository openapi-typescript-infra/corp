type: object
properties:
  messages:
    type: array
    description: Zero or more messages in the turn
    items:
      $ref: ../schemas/TurnMessage.yaml
  tool_responses:
    description: Responses to tool calls from a previous turn
    type: array
    items:
      $ref: ../../../packages/infra/tools/api/generated/AgentToolResponse.yaml
